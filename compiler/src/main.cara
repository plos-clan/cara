const fib_num = 10i64;

const main = extern C[main] fn() -> i64 {
    let mut a = "abcd";
    let mut b: [i64; 3] = test();
    let mut b_0 = 0i64;
    test_unit();
    test();
    b[0] = fib(fib_num);
    b[0]
};

const test_unit = fn() -> () {
    let mut test_var = &();
    *test_var
};

const fib = fn(n: i64) -> i64 {
    if n <= 1i64 {
        n
    } else {
        fib(n - 1i64) + fib(n - 2i64)
    }
};

const test = fn() -> [i64; 3] {
    [1i64, 2i64, 3i64]
};
