const fib_num = 10i64;

const main = extern C[main] fn() -> i64 {
    test();
    fib(fib_num)
};

const fib = fn(n: i64) -> i64 {
    if n <= 1i64 {
        n
    } else {
        fib(n - 1i64) + fib(n - 2i64)
    }
};

const test = fn() {
};
