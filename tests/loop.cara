const io = mod "utils/io.cara";

const main = extern C[main] fn() -> i32 {
    let mut i = 0;

    while i < 10 {
        io::print_num(i as i64);
        if i > 5 + 1 {
            break;
        };
        i = i + 1;
    };
    0
};
